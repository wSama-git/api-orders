# API Orders

API REST para gerenciamento de pedidos usando Node.js e Express.

## Instalação

npm install

## Rodar servidor

node server.js

## Endpoints

POST /order - criar pedido  
GET /order/list - listar pedidos  
GET /order/:id - buscar pedido  
DELETE /order/:id - deletar pedido
